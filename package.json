{
  "name": "bitstarter",
  "version": "0.0.1",
  "private" : true,
  "dependencies": {
    "express": "3.1.x"
  },
  "scripts" : {
    "start": "forever start -c node app.js",
    "test": "mocha --timeout 5000 --report list"
  },
  "engines": {
    "node": "0.10.x",
    "npm": "1.2.x"
  }
}
